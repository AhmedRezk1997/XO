import 'package:flutter/material.dart';

class MainColor {
  static Color primaryColor = Colors.teal.shade400;
  static Color secondaryColor = Colors.teal.shade700;
  static Color accentColor = Colors.teal.shade900;
  // static Color primaryColor = Color(0xFF00061a);
  // static Color secondaryColor = Color(0xFF003049);
  // static Color accentColor = Color(0xFF4169e8);
}
